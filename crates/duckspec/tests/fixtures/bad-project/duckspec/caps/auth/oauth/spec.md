# Google OAuth

Valid nested cap spec.

## Requirement: OAuth callback

Handle the callback.

> test: code

### Scenario: Valid callback

- **WHEN** the callback arrives
- **THEN** the system creates a session
