# @ Authentication

## + Two-factor integration

When a user has 2FA enabled, the login flow adds a TOTP
verification step between password check and session creation.
See the `auth/two-factor` capability for enrollment and
verification details.
